# One VM per line: <libvirt-name> <libvirt-uuid> <bmc-listen-ip> <bmc-mac> <chassis-serial>
# - bmc-listen-ip: an unused address on the sushy host's management LAN; deploy.sh adds it as an alias
# - bmc-mac: synthetic, unique, must not equal any VM NIC MAC (NiCo keys the ExpectedMachine on it)
# - chassis-serial: [A-Za-z0-9_-]{4,64}; redefine-vms.sh writes it into the domain's SMBIOS
# Keep the order in sync with NICO_BMC_IPS in nico/config/site.env.
nico-vm-1 00000000-0000-0000-0000-000000000001 198.51.100.202 02:00:00:00:0b:01 NICO-VM-1
nico-vm-2 00000000-0000-0000-0000-000000000002 198.51.100.203 02:00:00:00:0b:02 NICO-VM-2
